# Kata Camp

Static site for Kata Camp events and mission, built with Eleventy and deployed on Cloudflare Pages.

## Build

`bin/docker-build.sh` - runs install + `yarn build` in a Node container
`bin/docker-dev.sh` - runs dev container
